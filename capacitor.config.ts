import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'resto.example.com',
  appName: 'MCA_RESTO_WALE',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
